# Editmode tests (pass)

Single Editmode test that calls `Assert.Pass`. Used to verify Editmode test run, UTP/test results, and that the job summary can show a test table when Unity emits TestStatus. **Expected:** Tests pass.
