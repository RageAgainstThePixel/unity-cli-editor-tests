# Build warnings

An Editor script runs at build time (`IPreprocessBuildWithReport`) and logs a warning. Used to verify build-time **warnings** appear in logs/summary. **Expected:** Build succeeds; warning in log.
