# Compiler warnings

This project compiles with a single deliberate compiler warning (CS0618: call to obsolete member). Used to verify unity-cli and CI report compiler **warnings** in logs and job summary when UTP emits them. **Expected:** Build succeeds; warning appears in compiler output.
