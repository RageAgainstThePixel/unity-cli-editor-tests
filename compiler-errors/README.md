# Compiler errors

This project contains a `#error` preprocessor directive so it does not compile. Used to verify unity-cli and CI report compiler **errors** and fail the step. **Expected:** Build fails.
